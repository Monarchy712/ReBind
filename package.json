{
  "name": "rebind",
  "version": "1.0.0",
  "description": "Recovery infrastructure for compliance-restricted RWAs",
  "main": "deck.js",
  "scripts": {
    "start": "node backend/server.js",
    "build": "npm run compile && npm run web:install && npm run web:build",
    "compile": "hardhat compile",
    "compile:offline": "node scripts/compile-local.js",
    "test": "hardhat test",
    "test:offline": "hardhat test --no-compile",
    "deploy": "hardhat run scripts/deploy.js --network baseSepolia",
    "node:local": "hardhat node",
    "fund:local": "node scripts/fund-local.js",
    "deploy:local": "hardhat run scripts/deploy.js --network localdemo",
    "server:local": "node -e \"process.env.DEMO_MODE='local';require('./backend/server.js')\"",
    "register": "node scripts/register-atoken.js",
    "freeze-test": "node scripts/freeze-scope-test.js",
    "server": "node backend/server.js",
    "web": "npm --prefix frontend run dev",
    "web:build": "npm --prefix frontend run build",
    "web:install": "npm --prefix frontend install"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "engines": {
    "node": ">=20"
  },
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox": "^5.0.0",
    "hardhat": "^2.29.0",
    "solc": "^0.8.24"
  },
  "dependencies": {
    "@openzeppelin/contracts": "^5.0.2",
    "cors": "^2.8.6",
    "dotenv": "^17.4.2",
    "express": "^5.2.1"
  }
}
